import { createTheme, styled } from "@mui/material/styles";
export const themeInput = createTheme({
  palette: {
    primary: {
      main: "#010066",
      dark: "#010066",
    },
    secondary: {
      main: "#f50057",
    },
  },
});
